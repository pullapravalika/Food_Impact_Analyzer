# Configuration settings for the Food Impact Analyzer backend

class Config:
    
    SECRET_KEY = "food_impact_secret_key"

    # Example database configuration (can be used later)
    DB_HOST = "localhost"
    DB_USER = "root"
    DB_PASSWORD = ""
    DB_NAME = "food_impact_db"

    DEBUG = True